const API='https://discord.com/api/v10';
export async function discordFetch<T>(path:string,init:RequestInit={},token?:string):Promise<T>{const r=await fetch(`${API}${path}`,{...init,headers:{'Content-Type':'application/x-www-form-urlencoded',...(init.headers||{}),...(token?{Authorization:`Bearer ${token}`}:{})},cache:'no-store'});if(!r.ok)throw new Error(`Discord API ${r.status}`);return r.json() as Promise<T>}
export async function exchangeCode(code:string){const body=new URLSearchParams({client_id:process.env.DISCORD_CLIENT_ID!,client_secret:process.env.DISCORD_CLIENT_SECRET!,grant_type:'authorization_code',code,redirect_uri:process.env.DISCORD_REDIRECT_URI!});return discordFetch<{access_token:string;refresh_token:string;expires_in:number}>('/oauth2/token',{method:'POST',body});}
